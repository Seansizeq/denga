/**
 * «Приховати баланс»: коли режим увімкнено, кожна сума в застосунку
 * показується крапками замість цифр.
 *
 * Прапорець живе поза React навмисно — його читають чисті функції
 * форматування (`formatCurrency` і компанія), які викликаються з десятків
 * місць без доступу до контексту. За стан, збереження й перерендер відповідає
 * `LanguageProvider`: він тримає те саме значення в React-стані і синхронізує
 * цей прапорець на кожному рендері, до того як відрендеряться нащадки.
 */

/** Заміна для суми: чотири крапки замість числа. */
export const MONEY_MASK = '••••';

/** Коротка заміна для похідних чисел — відсотків зміни тощо. */
export const SHORT_MASK = '••';

let hidden = false;

export const setMoneyHiddenFlag = (next: boolean): void => {
  hidden = next;
};

export const isMoneyHidden = (): boolean => hidden;
