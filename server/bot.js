/**
 * Точка входу бот-процесу.
 *
 * Опитування Telegram, планувальники (звіти, нагадування, автосписання
 * підписок), розбір черги вихідних повідомлень, малювання карток і щоденні
 * бекапи. HTTP тут не піднімається.
 *
 * **Цей процес має бути рівно один.** Два опитування на один токен Telegram
 * розподіляє між собою навперемін — тобто половина повідомлень людей не
 * дійде до жодного з них. У `ecosystem.config.cjs` тому `instances: 1` і
 * `exec_mode: 'fork'`, а не `cluster`.
 */
process.env.DENGA_ROLE = 'bot';
await import('./index.js');
