/**
 * Точка входу процесу API.
 *
 * Тільки HTTP: ні опитування Telegram, ні планувальників, ні малювання звітів.
 * Саме тому цей процес можна запускати кількома копіями (`pm2 cluster`) —
 * жодна його частина не мусить бути єдиною в системі.
 *
 * Повідомлення в Telegram звідси не шлються напряму: вони лягають у
 * `telegram_outbox`, звідки їх забирає бот-процес (див. `telegram-outbox.js`).
 */
process.env.DENGA_ROLE = 'api';
await import('./index.js');
